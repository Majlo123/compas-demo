api:
  insecure: true
  dashboard: true

entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

log:
  level: DEBUG

providers:
  file:
    filename: /etc/traefik/dynamic_conf.yml
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false